def test_addition():
    # is it true that 2 + 2 equals 4?
    # if yes, return True, then the test passes
    # if no, raise an AssertionError, then the test fails
    assert 2 + 2 == 4, "Addition test failed"